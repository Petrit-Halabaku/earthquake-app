{
  "name": "seismos",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pretest": "esbuild src/data/quakeUtils.ts --bundle --format=esm --platform=node --outfile=tests/_utils.mjs",
    "test": "node --test 'tests/*.test.mjs'",
    "generate:landdots": "node scripts/generate-land-dots.mjs"
  },
  "dependencies": {
    "echarts": "^5.5.1",
    "gsap": "^3.12.5",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
